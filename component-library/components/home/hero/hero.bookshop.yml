# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: Home Hero
  description: Home Hero section
  icon: "cottage"
  tags: ["hero"]

# Defines the structure of this component, as well as the default values
blueprint:
  title: FTSK Barlangkutató Szakosztály
  description: Rövid bemutatkozó szöveg az egyesület főoldalához.
  link:
    text: "Try Free"
    url: "/#"

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:


# The banner visual is a slideshow sourced from data/hero_images.yaml (hugo.Data.hero_images),
# not a per-instance field - see that file to add/replace hero photos.
_inputs:
  description:
    type: markdown
