spec:
  structures:
    - content_blocks
  label: Tagnévsor
  description: Members grouped by membership category
  icon: groups
  tags: ["members"]

blueprint:
  title: Tagjaink
  title_suffix:
  description:

preview:

_inputs:
  description:
    type: markdown
  # The actual roster lives in data/members.yaml (single source of truth, also
  # used to look up trip-report participants) - not editable per content block.
